package cn.hm1006.boot.mapper;import cn.hm1006.boot.entity.Air;import com.github.pagehelper.PageHelper;import com.github.pagehelper.PageInfo;import org.junit.Test;import org.junit.runner.RunWith;import org.springframework.boot.test.context.SpringBootTest;import org.springframework.test.context.junit4.SpringRunner;import javax.annotation.Resource;import java.util.List;/** * @author : Two * @version : v1.0 * @description TODO * @date : 2021/3/1 17:37 */@SpringBootTest@RunWith(SpringRunner.class)public class AirMapperTest {    @Resource    private AirMapper airMapper;    @Test    public void findAll(){        List<Air> all = airMapper.findAll();        for (Air air : all) {            System.out.println(air.toString());        }    }    @Test    public void findById(){        Air air = airMapper.findbyId(1);        System.out.println(air);    }    @Test    public void findAirPage(){        //pageNum起始页（当前页码） pageSize页面显示的条数        PageHelper.startPage(1,5);        List<Air> all = airMapper.findAll();        PageInfo<Air> info = new PageInfo<>(all);        List<Air> airList = info.getList();        for (Air air : airList) {            System.out.println(air);        }    }}